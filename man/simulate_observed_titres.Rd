% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/titre_trajectories.R
\name{simulate_observed_titres}
\alias{simulate_observed_titres}
\title{Simulate Observed Titres with Gaussian Measurement Error}
\usage{
simulate_observed_titres(true_titre, measurement_error, LOD)
}
\arguments{
\item{true_titre}{Numeric vector. True underlying titre values.}

\item{measurement_error}{Numeric scalar. Standard deviation of the Gaussian measurement error.}

\item{LOD}{Numeric scalar. Limit of detection below which observed titres are set to zero.}
}
\value{
A numeric vector of observed titres after adding measurement error
  and applying the LOD rule.
}
\description{
This function simulates observed titre values by adding Gaussian (normal)
measurement error to true underlying titres. Any observed value falling below
a specified limit of detection (LOD) is reported as zero.
}
\details{
Observations are generated as:
\deqn{obs = true\_titre + \epsilon,\quad \epsilon \sim \mathcal{N}(0, \sigma^2)}
where `measurement_error` is the standard deviation \eqn{\sigma} of the noise.
Any value less than `LOD` is returned as 0, mimicking assay detection limits.
}
\examples{
set.seed(123)
true <- c(5, 10, 2, 1)
simulate_titre_observation(true_titre = true,
                           measurement_error = 1.5,
                           LOD = 1)

}
